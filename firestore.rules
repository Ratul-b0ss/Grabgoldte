
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow only authenticated users to read and write to the database
    // This allows the referral system (writing to other users' arrays) to work
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
